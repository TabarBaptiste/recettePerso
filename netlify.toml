# Netlify configuration for Recipe Management Frontend

[build]
  base = "frontend"
  publish = "dist/frontend/browser"
  command = "npm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "22"
